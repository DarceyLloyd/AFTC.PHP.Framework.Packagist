// Call functions in here to handle resize tasks
window.onresize = function(event) {
	
}

// Call functions in here to handle page scroll tasks
$(window).scroll(function(){
	
});


// Call functions in here, executes when page has loaded / is ready
$( document ).ready(function( $ ) {

});